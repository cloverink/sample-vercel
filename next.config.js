const nextConfig = {
  reactStrictMode: true,
  env: {
    foo: process.env.foo,
    hello: process.env.hello,
    hello1: process.env.hello1,
    aaa: process.env.aaa
  }
}

module.exports = nextConfig
